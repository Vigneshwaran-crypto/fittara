import React from "react";

// import * as THREE from "three";

const Orders = (props) => {
  // const { camera, gl } = useThree();
  return (
    <div style={{ display: "flex", height: "100%", width: "100%" }}>Orders</div>
  );
};

export default Orders;
